# String methods

name = input("Enter your name: ")

# result = len(name)
# result = name.find("m") # finds the first occurence of the given string
# result = name.rfind("ob") #finds the last occurence of the given string

# name = name.capitalize()# only first letter is uppercased
# name = name.upper() # all letters are uppercased
# name = name.lower()

# result = name.isdigit() #checks whether all characters are digits
# result = name.isalpha() #checks whether all characters are alphabets or not

phone_number = "123-456-789"
# result = phone_number.count("-")
result = phone_number.replace("-","")

print(result)
# print(name)

print(help(bool))